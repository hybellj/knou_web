<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="knou.lms.smnr.dao.SmnrTeamDAO">

	<insert id="smnrTeamBulkRegist" parameterType="java.util.List">
		/*
             SQL ID : SmnrTeamDAO.smnrTeamBulkRegist
             설  명 : 세미나팀일괄등록
         */
         INSERT ALL
		<foreach item="item" collection="list">
			INTO TB_LMS_SMNR_TEAM (
	         	  SMNR_TEAM_ID
				, SMNR_ID
				, TEAM_ID
				, RGTR_ID
				, REG_DTTM
	         ) VALUES (
	         	  #{item.smnrTeamId}
				, #{item.smnrId}
				, #{item.teamId}
				, #{item.rgtrId}
				, FN_DTTM()
	         )
		</foreach>
		SELECT * FROM DUAL
	</insert>

	<delete id="smnrTeamBulkDelete" parameterType="String">
		/*
             SQL ID : SmnrTeamDAO.smnrTeamBulkDelete
             설  명 : 세미나팀일괄삭제
         */
        DELETE FROM TB_LMS_SMNR_TEAM
         WHERE SMNR_ID IN (SELECT SMNR_ID FROM TB_LMS_SMNR WHERE UP_SMNR_ID = #{smnrId})
	</delete>

</mapper>