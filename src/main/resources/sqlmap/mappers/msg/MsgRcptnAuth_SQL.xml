<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="knou.lms.msg.dao.MsgRcptnAuthDAO">

    <sql id="rcvynCondition">
        <choose>
            <when test="chnlCd == 'SHRTNT'">
                NVL(U.SHRTNT_ALIM_RCVYN, 'Y')
            </when>
            <when test="chnlCd == 'EML'">
                NVL(U.EML_ALIM_RCVYN, 'Y')
            </when>
            <when test="chnlCd == 'SMS'">
                CASE WHEN SUBSTR(NVL(U.PUSH_TALK_SMS_FLAG, '0000000111'), 1, 1) = '1' THEN 'Y' ELSE 'N' END
            </when>
            <when test="chnlCd == 'PUSH'">
                CASE WHEN SUBSTR(NVL(U.PUSH_TALK_SMS_FLAG, '0000000111'), 4, 1) = '1' THEN 'Y' ELSE 'N' END
            </when>
            <when test="chnlCd == 'ALIM_TALK'">
                CASE WHEN SUBSTR(NVL(U.PUSH_TALK_SMS_FLAG, '0000000111'), 6, 1) = '1' THEN 'Y' ELSE 'N' END
            </when>
            <otherwise>
                'Y'
            </otherwise>
        </choose>
    </sql>

    <select id="selectRcptnPrmNoUserIdList" parameterType="map" resultType="string">
        /*
             SQL ID : MsgRcptnAuthDAO.selectRcptnPrmNoUserIdList
             설  명 : 채널별 수신 미허용 사용자 ID 목록 조회
         */
        SELECT U.USER_ID
          FROM TB_LMS_USER U
         WHERE U.USER_ID IN
            <foreach collection="userIds" item="id" open="(" separator="," close=")">#{id}</foreach>
           AND <include refid="rcvynCondition"/> = 'N'
    </select>

    <select id="selectRcptnPrmYn" parameterType="map" resultType="string">
        /*
             SQL ID : MsgRcptnAuthDAO.selectRcptnPrmYn
             설  명 : 단건 수신 허용 여부 조회
         */
        SELECT <include refid="rcvynCondition"/> AS RCVYN
          FROM TB_LMS_USER U
         WHERE U.USER_ID = #{userId}
    </select>

</mapper>
